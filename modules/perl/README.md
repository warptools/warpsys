This is **not repeatable** due to the OSVERS value depending on the kernel. On Arch,  this is the full `uname -r` string. On Ubuntu it is always `linux-generic`.

TODO: figure out how to override OSVERS.`
