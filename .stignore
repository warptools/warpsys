// note: this file and the '.gitignore' file are closely related.

// don't sync large assets.
/.warpforge/cache/
/.warpforge/warehouse/

// memoization shouldn't be shared by default either.
/.warpforge/memos/

// definitely don't version or share the working dirs used at runtime.
/.warpforge/runc-root/

// the catalogs dir *is* shared, in the way we typically use syncthing at hacktime.
// /.warpforge/catalogs/

// as a loose convention, these file paths are usable by others for notes, and aren't synced:
/_*notes*.md

// but the "swap" dir should be synced, because that's it's whole point:
// /_swap/
